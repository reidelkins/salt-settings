

export const calculateSettings = (selectedMachine, selectedSetting, waterHardness, compensatorySalt) => {
    console.log(selectedMachine, selectedSetting, waterHardness, compensatorySalt)
    return 1;
};